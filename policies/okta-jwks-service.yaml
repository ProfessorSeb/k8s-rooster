###############################################################################
# AgentgatewayBackend for Okta JWKS endpoint
# Used by MCP authentication policies to validate JWT signatures.
###############################################################################
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: okta-jwks
  namespace: agentgateway-system
  labels:
    category: authentication
spec:
  static:
    host: integrator-7147223.okta.com
    port: 443
