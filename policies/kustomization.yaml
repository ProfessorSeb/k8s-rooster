apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # - github-mcp-read-only-policy.yaml        # 50% GitHub tools blocked (read-only)
  - github-mcp-safe-ops-policy.yaml            # ~25% GitHub tools blocked (no destructive ops)
  - mcp-block-echo-getenv-policy.yaml          # Block echo + get-env on MCP

commonLabels:
  app.kubernetes.io/name: agentgateway-policies
  app.kubernetes.io/component: demo-policies

namespace: agentgateway-system
