apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Comment/uncomment resources to enable/disable policies for demos
resources:
  # Uncomment ONE of these policies at a time for demos:
  # - github-mcp-read-only-policy.yaml    # 50% tools blocked (read-only)
  - github-mcp-safe-ops-policy.yaml       # ~25% tools blocked (no destructive ops)

commonLabels:
  app.kubernetes.io/name: agentgateway-policies
  app.kubernetes.io/component: demo-policies

namespace: agentgateway-system
