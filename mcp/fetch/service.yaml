apiVersion: v1
kind: Service
metadata:
  name: fetch-mcp-server
  namespace: agentgateway-system
  annotations:
    kgateway.dev/mcp-path: /fetch
spec:
  selector:
    app: fetch-mcp-server
  ports:
  - port: 3010
    targetPort: 3010
    protocol: TCP
    name: http