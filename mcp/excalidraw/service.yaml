apiVersion: v1
kind: Service
metadata:
  name: excalidraw-mcp
  namespace: agentgateway-system
  labels:
    app: excalidraw-mcp
spec:
  selector:
    app: excalidraw-mcp
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  type: ClusterIP