apiVersion: v1
kind: Service
metadata:
  name: excalidraw-mcp
  namespace: agentgateway-system
  labels:
    app: excalidraw-mcp
spec:
  selector:
    app: excalidraw-mcp
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP