apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: excalidraw-mcp
  namespace: agentgateway-system
spec:
  static:
    host: excalidraw-mcp.agentgateway-system.svc.cluster.local
    port: 3001